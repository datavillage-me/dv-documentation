get:
  summary: Get your current credentials
  tags:
    - auth
  responses:
    "200":
      description: A JSON array of collaboration spaces
      content:
        application/json:
          schema:
            type: object
            required:
              - isAdmin
            properties:
              isAdmin:
                type: boolean
