get:
  summary: Get all spaces your account has access to
  tags:
    - spaces
  responses:
    "200":
      description: A JSON array of collaboration spaces
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../schemas/CollaborationSpace.yaml"
