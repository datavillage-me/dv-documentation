get:
  summary: Get deployed cages.
  tags:
    - crud
  responses:
    "200":
      description: A JSON array of cages
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../schemas/Cage.yaml"
